import {driverEndpoints} from "./driver-endpoints"
import {rideEndpoints} from "./ride-endpoints"
import {riderEndpoints} from "./rider-endpoints"

export const endpoints = {
  rider: riderEndpoints,
  driver: driverEndpoints,
  ride: rideEndpoints,
}
